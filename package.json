{
  "name": "nursing-study-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "דשבורד מעקב לימודים — סיעוד המבוגר (static site, no build step; deps are for tests only)",
  "scripts": {
    "test": "node tests/smoke.test.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
